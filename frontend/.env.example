# Em desenvolvimento com Vite, deixe vazio para usar o proxy (/api -> backend).
# Em produção (Vercel), use a URL pública da API no Render:
# VITE_API_URL=https://seu-servico.onrender.com/api
VITE_API_URL=
